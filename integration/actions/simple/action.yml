name: 'simple integration'
description: 'Testing actions for workflow composition'

runs:
  using: "composite"
  steps:
  - name: First step
    shell: bash
    run: echo "First step simple integration"
  - name: Second step
    shell: bash
    run: echo "Second step simple integration"
