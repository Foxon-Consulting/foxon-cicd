name: Integration

on:
  workflow_call:

jobs:
  lint:
    # echo Hello World
    runs-on: ubuntu-latest
    steps:
      - name: simple
        uses: Foxon-Consulting/github-actions/integration/simple/integration_step@main
