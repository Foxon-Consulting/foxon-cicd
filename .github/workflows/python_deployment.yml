name: Deployment

on:
  workflow_call:

jobs:
  lint:
    # echo Hello World
    runs-on: ubuntu-latest
    steps:
      - name: simple
        uses: Foxon-Consulting/github-actions/deployment/actions/simple@main
    # TODO